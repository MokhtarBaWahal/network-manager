fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy==2.0.25
python-dotenv==1.0.0
pydantic==2.6.4
pydantic-settings==2.2.1
alembic==1.13.1

# Database - Use psycopg[binary] instead of psycopg2-binary for better Windows support
psycopg[binary]==3.1.18

httpx==0.26.0
pyyaml==6.0.1
python-dateutil==2.8.2
pytz==2024.1

# Starlink client library - using local path from project
# To use: pip install -e ../../../starlink-client/libs/python/starlink-client

# MikroTik API - binary protocol for RouterOS v6, REST for v7+
routeros-api==0.18.0

# Async support
aiohttp==3.9.3

# VPN/Networking
# wireguard-tools (installed via OS package, not pip)

# Security & Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pydantic[email]==2.6.4
python-multipart==0.0.9

# Monitoring & Logging
python-json-logger==2.0.7

# Testing
pytest==7.4.4
pytest-asyncio==0.23.2
